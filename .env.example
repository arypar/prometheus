# ═══════════════════════════════════════════════════════════
#  PROMETHEUS — Environment Variables
# ═══════════════════════════════════════════════════════════
#
#  Three deployment targets:
#    1. Vercel    → frontend (app/)
#    2. Railway   → API server (backend/src/index.ts)
#    3. Railway   → Worker process (backend/src/worker.ts)
#
# ───────────────────────────────────────────────────────────
#  VERCEL — Frontend (set in Vercel dashboard → Settings → Environment Variables)
# ───────────────────────────────────────────────────────────
NEXT_PUBLIC_API_URL=https://your-railway-api.up.railway.app/api
NEXT_PUBLIC_EXPLORER_URL=https://monad.socialscan.io
NEXT_PUBLIC_WALLET_ADDRESS=0x...

# ───────────────────────────────────────────────────────────
#  RAILWAY — Shared by both API and Worker services
#  (set once in a Railway shared variable group, or per-service)
# ───────────────────────────────────────────────────────────

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-service-role-key

# Monad chain
MONAD_NETWORK=mainnet
MONAD_RPC_URL=https://monad-mainnet.drpc.org
MOLTBOT_WALLET_ADDRESS=0x...

# nad.fun
NAD_FUN_API_URL=https://api.nadapp.net
NAD_FUN_API_KEY=

# Bot security
BOT_API_KEY=your-secret-bot-api-key

# AI (portfolio manager)
ANTHROPIC_API_KEY=sk-ant-api03-...

# Wallet (needed by Worker only, but safe to set on both)
PROMETHEUS_PRIVATE_KEY=0x...

# CORS — set to your Vercel frontend URL (comma-separated for multiple)
CORS_ORIGIN=https://your-app.vercel.app

# Port — Railway sets this automatically, no need to set manually
# PORT=4000
